import { ArrayFromRangePipe } from './array-from-range.pipe';

describe('ArrayFromRangePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFromRangePipe();
    expect(pipe).toBeTruthy();
  });
});
